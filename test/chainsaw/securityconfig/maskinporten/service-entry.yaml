apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: security-config-maskinporten-f542b998
  labels:
    accesserator.kartverket.no/controller: securityconfig
    app.kubernetes.io/managed-by: accesserator
spec:
  exportTo:
    - .
    - istio-gateway
    - istio-system
  hosts:
    - test.maskinporten.no
  ports:
    - name: https
      number: 443
      protocol: HTTPS
  resolution: DNS
