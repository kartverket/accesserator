apiVersion: accesserator.kartverket.no/v1alpha
kind: SecurityConfig
metadata:
  name: security-config
spec:
  applicationRef: app
  tokenx:
    enabled: true
status:
  jwkerSecretName: app-jwker-secret
  message: SecurityConfig ready.
  phase: Ready
  ready: true
