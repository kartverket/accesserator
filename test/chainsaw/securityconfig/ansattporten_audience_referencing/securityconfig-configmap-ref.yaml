apiVersion: accesserator.kartverket.no/v1alpha
kind: SecurityConfig
metadata:
  name: security-config
spec:
  applicationRef: app
  ansattporten:
    enabled: true
    allowedAudience:
      valueFrom:
        configMapKeyRef:
          name: ansattporten-audience-config
          key: AUDIENCE
