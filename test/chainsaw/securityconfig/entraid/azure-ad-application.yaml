apiVersion: nais.io/v1
kind: AzureAdApplication
metadata:
  name: app
spec:
  secretName: security-config-entraid
status:
  synchronizationSecretName: security-config-entraid
  synchronizationState: Synchronized
