apiVersion: accesserator.kartverket.no/v1alpha
kind: SecurityConfig
metadata:
  name: security-config
spec:
  applicationRef: app
status:
  message: "failed to fetch Application resource named app: Application.skiperator.kartverket.no \"app\" not found"
  phase: Invalid
  ready: false
