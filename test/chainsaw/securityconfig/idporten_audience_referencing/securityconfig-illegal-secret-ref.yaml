apiVersion: accesserator.kartverket.no/v1alpha
kind: SecurityConfig
metadata:
  name: security-config
spec:
  applicationRef: app
  idporten:
    enabled: true
    allowedAudience:
      valueFrom:
        secretKeyRef:
          name: empty-audience-secret
          key: AUDIENCE
