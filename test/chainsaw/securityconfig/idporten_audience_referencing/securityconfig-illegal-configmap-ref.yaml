apiVersion: accesserator.kartverket.no/v1alpha
kind: SecurityConfig
metadata:
  name: security-config
spec:
  applicationRef: app
  idporten:
    enabled: true
    allowedAudiences:
      - valueFrom:
          configMapKeyRef:
            name: empty-audience-configmap
            key: AUDIENCE
