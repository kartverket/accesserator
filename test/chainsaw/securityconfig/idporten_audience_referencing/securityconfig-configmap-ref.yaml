apiVersion: accesserator.kartverket.no/v1alpha
kind: SecurityConfig
metadata:
  name: security-config
spec:
  applicationRef: app
  idporten:
    enabled: true
    allowedAudience:
      valueFrom:
        configMapKeyRef:
          name: idporten-audience-config
          key: AUDIENCE
