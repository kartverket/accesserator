apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Configuration
metadata:
  name: configuration
spec:
  parallel: 1
  timeouts:
    delete: 60s
    assert: 60s
    apply: 60s
    cleanup: 60s
    error: 60s
    exec: 90s
